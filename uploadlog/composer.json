{
    "name": "ft/uploadlog",
    "description": "FT98: File upload — MIME validation, size limits, path traversal field trial",
    "type": "project",
    "require": {
        "php": ">=8.4.1",
        "hideyukimori/nene2": "^1.5"
    },
    "require-dev": {
        "phpunit/phpunit": "^11",
        "phpstan/phpstan": "^2",
        "friendsofphp/php-cs-fixer": "^3"
    },
    "autoload": {
        "psr-4": {
            "Upload\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Upload\\Tests\\": "tests/"
        }
    },
    "scripts": {
        "test": "phpunit --colors=always",
        "analyse": "phpstan analyse --level=8 --memory-limit=512M src tests",
        "cs": "php-cs-fixer fix --dry-run --diff",
        "cs:fix": "php-cs-fixer fix",
        "check": ["@cs", "@analyse", "@test"]
    },
    "config": {
        "optimize-autoloader": true,
        "sort-packages": true
    }
}
